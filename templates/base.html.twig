<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}GameRating{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/milligram/1.4.1/milligram.min.css">
        {# CSS vis.js #}
        <link href="https://unpkg.com/vis-timeline@7.7.0/styles/vis-timeline-graph2d.min.css" rel="stylesheet" />
        {# JS vis.js #}
        <script src="https://unpkg.com/vis-timeline@7.7.0/standalone/umd/vis-timeline-graph2d.min.js"></script>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
    {% if app.request.attributes.get('_route') != 'app_login' and app.request.attributes.get('_route') != 'app_register' %}
        <header>
            <nav class="navbar">
                <div><a href="{{ path('app_home') }}">📂 Menu principal</a></div>
                <div class="navLeft"><a href="{{ path('app_user_game_index') }}">🎮 Mes jeux</a></div>
                <div class="navLeft"><a href="{{ path('app_game_index') }}">🎮 Les jeux</a></div>
                <div class="navLeft"><a href="{{ path('app_profile_edit') }}" >👤 Mon Compte</a></div>
                <div class="navRight"><a href="{{ path('app_logout') }}" >❌ Déconnexion</a></div>
            </nav>
        </header>
    {% endif %}
        {% block body %}{% endblock %}
    </body>
</html>
